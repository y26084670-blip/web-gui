# Clark UI Editor

Обновлено: 2026-09-09 12:20 UTC.

Текущая версия: **4.15.0**.

Clark UI Editor — браузерное приложение для выбора базового каталога проектов, 
проекта и задания, чтения файлов из input3XX, редактирования скаляров и массивов, 
операций над записями и строками, проверки ограничений, прикладной диагностики 
и сохранения исходных данных. Приложение собирается Vite и работает полностью 
на стороне браузера.

Выбор проекта и задания выполняется на вкладке «Выбор задания».

Перед записью модели редактор создаёт `input3XX/_nogo.e3d`. Маркер удаляется
только после полного сохранения и новой проверки без ошибок; его наличие
предупреждает решатель о неподтверждённых исходных данных, но не блокирует расчёт.

## Требования

Для сборки и разработки:

- Node.js 24; состав и диапазоны зависимостей заданы в
  [package.json](package.json), точные версии дерева — в
  [package-lock.json](package-lock.json).

Для работы редактора на клиентском компьютере:

- безопасный контекст: localhost либо HTTPS;
- необходимые методы File System Access API для доступа к локальному каталогу;
- разрешение пользователя на чтение и запись выбранного локального каталога.

Полная функциональность редактора гарантируется для браузеров Google Chrome
или Microsoft Edge. Это рекомендация по выбору браузера: запрета по его
названию нет. Отдельный Chromium SDK на клиентском компьютере не устанавливается;
необходимые Web API предоставляет сам браузер.

Перед открытием системного файлового диалога редактор проверяет безопасный
контекст и наличие необходимых методов File System Access API. Если условия не
выполнены, показывается сообщение о недоступной возможности и рекомендации
по браузеру и адресу.

В обычном режиме необходимо выбирать корневой каталог с точным именем
`clark.projects`. Он может находиться на любом диске и на любой глубине.
Сеансовый режим администратора снимает только это ограничение имени.

Установка зависимостей из корня репозитория. Выполняет «чистую установку» зависимостей 
проекта строго по package-lock.json, удаляя существующую папку node_modules и гарантируя 
воспроизводимость окружения. Это быстрее и надёжнее, чем npm install.

~~~bat
npm ci
~~~

Подробное сравнение режимов разработки и релиза, последовательность сборки,
варианты запуска и типовые расхождения описаны в
[справочнике по сборке и выполнению](docs/BUILD_AND_RUNTIME.md).

## Общая информация и установка

При открытии или обновлении страницы автоматически показывается «Общая
информация». Снимите флажок «Открывать эту панель при старте», чтобы отключить
автопоказ в данном браузере. Кнопка `?` открывает панель независимо от флажка.
Пункт 4 содержит быструю справку по установке и использованию решателя;
используемые размерности приведены в пункте 5.

«Установить примеры» копирует опубликованные на сервере задания в локальный
`clark.projects`. Выберите диск или родительскую папку либо существующий
`clark.projects`, дождитесь копирования и выберите этот каталог в редакторе.
Legacy-задания, актуальные данные и результаты сохраняются без преобразования.
Под кнопкой на её ширину отображается прогресс «Скачано 3 файла из N»:
`N` определяется по списку файлов опубликованного комплекта. Счётчик
увеличивается после сохранения очередного файла либо пропуска уже существующего.
Отдельного счётчика существующих файлов и прежнего блока сообщений о ходе
скачивания нет. При отмене уже записанные файлы остаются; повторная установка
допишет недостающие.

«Скачать установщик решателя» открывает в новой вкладке
[облачный каталог с актуальной версией](https://cloud.mail.ru/public/nLj8/CVS8JAToQ).
Выберите установочный файл и скачайте его средствами облачного сервиса.
Размер и прогресс загрузки отображает сервис или браузер; в редакторе
отдельного индикатора и сообщения о скачивании установщика нет.

Самостоятельно запустите самораспаковывающийся установщик и выберите корень
внутреннего стационарного диска. После установки в папке `Clark` на рабочем
столе находятся четыре ярлыка. Для запуска решателя перетащите подготовленный
файл со списком заданий на ярлык «Запуск расчета» либо откройте ярлык двойным
щелчком и выберите список в стандартном файловом диалоге.
По умолчанию в `runtime.json` задано четыре параллельных процесса (`mpiRanks: 4`);
число процессов выбирают с учётом числа физических ядер компьютера.
Краткая процедура приведена в панели; полная инструкция
принадлежит проекту [solver](https://github.com/y26084670-blip/solver/blob/main/README.md).

Для публикации примеров поместите их дерево в `public/examples/clark.projects`.
Ссылка на облачный каталог установщика задана в поле `solverInstallerUrl` файла
[public/examples-config.json](public/examples-config.json). Пока материалы не опубликованы, панель сообщает
об их недоступности. Настройка сервера и генерация списка файлов описаны в
[справочнике по сборке и выполнению](docs/BUILD_AND_RUNTIME.md#примеры-и-установщик-решателя).

## Заданные источники в 3D

В окне «3D геометрия» откройте «Общие опции» и отметьте «Заданные источники».
Геометрия автоматически переключится в полупрозрачный вид. Зелёные стрелки
показывают заданную намагниченность, красные — заданную плотность тока.

Кнопка ⚙ рядом с опцией открывает настройки: тонкие или объёмные стрелки
и отдельные ползунки видимой длины для плотности тока и намагниченности.
Масштаб меняется логарифмически от ×0,1 до ×10, начальное значение — ×1.
Кнопка сброса возвращает обе длины к ×1. Настройки меняют только отображение;
стрелки обновляются отдельно от поверхностей, значения источников сохраняются.

Стрелки размещаются в центрах независимых элементарных объёмов и соответствуют
строкам одноимённой вкладки. Показ обновляется по текущим значениям таблицы
и геометрии, включая несохранённые изменения. Используются исходное положение
элементов и значения источников без движения и временных множителей амплитуды.
Дополнительные образы физических симметрий и зеркальные копии источников
не создаются.

## Рабочие константы

Актуальный `input3XX/conrab.txt` содержит две строки данных: первую для Float32,
вторую для Float64. Если строка только одна, редактор выдаёт предупреждение,
загружает её значения в обе колонки и позволяет редактировать их независимо.
Вкладка сразу помечается как изменённая: файл нужно сохранить, даже если
значения вручную не редактировались. Undo не снимает этот признак; он снимается
после успешного сохранения обеих строк, независимо от выбранной разрядности.
При загрузке исходный файл не изменяется.

Логический параметр `StartITER` в «Конфигурация модели» сохраняется в JSON
как `startITER`: `true` задаёт нулевое начальное приближение итерационного
процесса, `false` использует значения с предыдущей итерации. По умолчанию,
в том числе при отсутствии поля в старом файле, используется `false`.
Значения Float32 и Float64 редактируются и сохраняются независимо.

На вкладке рабочих констант тонкая синяя рамка выделяет колонку разрядности,
выбранной в «Общие параметры → Разрядность вычислений». Изменение этого
параметра переключает рамку; значения другой колонки сохраняются.

## Сборка и запуск для отладки

Отдельный debug-bundle в dist/ не создаётся. Для разработки используется Vite
dev server:

~~~bat
npm run dev
~~~

На Windows можно запустить [scripts/dev.cmd](scripts/dev.cmd). Сценарий при
необходимости выполняет npm ci, затем запускает dev server и открывает браузер:

~~~bat
scripts/dev.cmd
~~~

## Сборка для релиза

Перед выпуском повышайте minor или patch по
[правилам версионирования](docs/IMPLEMENTATION_PROTOCOL.md), согласованно
обновляя `package.json` и корневые записи `package-lock.json`.
Сборочные команды номер версии автоматически не меняют.

Production-сборка создаётся в каталоге `dist`:

~~~bat
npm run build:release
~~~

Команда npm run build также выполняет production-сборку. Для GitHub Pages
используется отдельный base path:

~~~bat
npm run build:pages
~~~

## Публикация релиза на GitHub Pages без ИИ

Сборка и публикация выполняются GitHub автоматически. Для обновления уже
работающего сайта:

1. В GitHub Desktop выберите репозиторий `web-gui`, ветку `main` и получите
   актуальные изменения через `Fetch origin` / `Pull origin`.
2. Подготовьте изменения и обновите версию. Локальная контрольная сборка
   и тесты выполняются при необходимости; порядок для пути `/web-gui/`
   дан в подробном регламенте ниже.
3. Проверьте список файлов, выполните `Commit to main`, затем `Push origin`.
   Если нужный коммит уже находится в `main` на GitHub, повторный коммит не нужен.
4. Откройте [Actions → Build and deploy GitHub Pages](https://github.com/y26084670-blip/web-gui/actions/workflows/deploy-pages.yml)
   и выберите запуск с нужным коммитом. Дождитесь успешного завершения
   задания `deploy`, включая шаг `Deploy to GitHub Pages`.
5. Откройте сайт по ссылке окружения `github-pages` в результате запуска
   или через `Settings → Pages → Visit site`, обновите страницу и проверьте
   изменённый пользовательский сценарий. Рекомендуются Chrome или Edge.

Для повторной публикации текущего `main` без изменения файлов на странице
того же workflow нажмите `Run workflow`, выберите `main` и подтвердите запуск.

Локальный Node.js нужен для локальной сборки; при публикации его устанавливает
GitHub. Каталог `dist` формируется на GitHub из отправленных исходников.
Создавать GitHub Release или тег для этой процедуры не требуется.

Полный [регламент публикации релиза](docs/BUILD_AND_RUNTIME.md#публикация-релиза-на-github-pages)
содержит предварительные условия, команды, этапы workflow, проверку результата,
разбор сбоев и откат. Исполняющий сценарий:
[deploy-pages.yml](.github/workflows/deploy-pages.yml).

## Запуск готовой сборки

Локальная проверка `dist`, созданного через `build` или `build:release`:

~~~bat
npm run preview
~~~

Для результата `build:pages` используйте
`npm run preview -- --base=/web-gui/` и адрес
[http://127.0.0.1:4173/web-gui/](http://127.0.0.1:4173/web-gui/).

Локальная release-сборка на Windows с чистой установкой зависимостей:

~~~bat
scripts/build-local.cmd
~~~

Сценарий выполняет `npm ci` и `npm run build:release`.

Перед первым развёртыванием в локальной сети нужно установить Caddy
(сценарий использует winget):

~~~bat
scripts/caddy/install.cmd
~~~

Сборка и запуск release-варианта для локальной сети:

~~~bat
scripts/release-lan.cmd
~~~

Связанные сценарии находятся в [scripts/caddy/](scripts/caddy/), а конфигурация
сервера — в [deploy/caddy/Caddyfile](deploy/caddy/Caddyfile). LAN-сценарий
рассчитан на Windows, PowerShell, Caddy и частный сетевой профиль Windows.

## Тесты

Существующие тесты находятся в [test/](test/) и при необходимости запускаются
отдельно, без браузера:

~~~bat
npm test
~~~

Команда выполняет `node --test`. Запуск и состав проверок выбираются по риску
конкретного изменения; тестирование не является обязательным этапом каждой
правки. Локальные сценарии сборки и автоматическая публикация тесты не запускают.
В GitHub Actions их можно включить параметром `run_tests` при ручном запуске
workflow `Build editor and agent fallback`; по умолчанию параметр выключен.
Подготовка и проверка комплектности базовых библиотек при сборке сохраняются.
Текущее подтверждённое состояние проверок зафиксировано в
[docs/resume.md](docs/resume.md).

Архитектурный контракт находится в
[docs/Architecture.md](docs/Architecture.md), порядок внесения изменений — в
[docs/IMPLEMENTATION_PROTOCOL.md](docs/IMPLEMENTATION_PROTOCOL.md), текущее
состояние и открытые работы — в [docs/resume.md](docs/resume.md).

## Локальный помощник

В версии 4.12.0 крайняя правая панель полностью отключена: её элементы
недоступны для мыши и клавиатуры, автоматическое подключение к `clark.agent`
не выполняется. Статус: «Агент отключен. Встроенный помощник
[Экспериментальный: пока ничего не работает]».

Код базовых рекомендаций, 18 тем справки и интеграции сохранён для дальнейшей
работы. Технические сценарии и ограничения:
[локальная интеграция](docs/AGENT_INTEGRATION.md).
