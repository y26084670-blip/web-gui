//------------------------------------------------------------------------------
// Редактор пути.
// Если путь вводится вручную (без открытия системного диалога)
//------------------------------------------------------------------------------
import { createInputEditor } from "../editorFactory";

export const pathEditor =
    createInputEditor({

        htmlType: "text",

    });