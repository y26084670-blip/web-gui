//==============================================================================
// Инвариантные опции экземпляра Tabulator.
//
// Здесь объявляются только опции, зафиксированные инвариантами Architecture.md
// и обязательные для каждого экземпляра. Опции, относящиеся к конкретному виду
// представления (раскладка, режимы выделения, размеры), остаются собственностью
// создателя таблицы.
//
// Точки создания таблиц независимы: каждая распаковывает этот набор
// самостоятельно.
//==============================================================================

export const COMMON_TABLE_OPTIONS = Object.freeze({

    // Порядок исходных данных изменяется только явными операциями со строками.
    columnDefaults: {
        headerSort: false,
    },

    // Нарушение ограничения ячейки не блокирует ввод.
    validationMode: "highlight",

});