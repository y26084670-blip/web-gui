# Резюме проекта UI Editor

Обновлено: 2026-08-24 18:41 UTC.

Документ фиксирует текущее состояние и точку продолжения. История реализации
хранится в Git и Pull Request и здесь не повторяется.

## 1. Документы проекта

- `Architecture.md` — единственный архитектурный контракт;
- `IMPLEMENTATION_PROTOCOL.md` — порядок внесения изменений;
- `../README.md` — окружение, команды сборки, запуска и развёртывания;
- настоящий файл — реализованное состояние и открытые работы.

## 2. Текущее состояние приложения

Clark UI Editor является браузерным редактором исходных данных задания.
Приложение выбирает базовый каталог, проект и задание через File System Access
API, загружает данные из обязательного каталога `input3XX`, отображает и
редактирует их по схемам вкладок, выполняет диагностику и сохраняет изменения.

Реализованы:

- трёхуровневая модель StorageModel → BaseModel → Tabulator ViewModel;
- сериализация CLUSTER, RECORDS и ARRAY с `storageKey` и явным `order`;
- вариантные ARRAY-представления и общий граф `compute`/`computeStored`;
- схемы и вкладки основных групп исходных данных;
- constraint-валидация отдельных значений и model-валидация вкладок;
- диагностика загрузки и ручная проверка модели;
- основная и связанная таблицы, область деталей и computedView;
- вычисляемые колонки элементов и раскладка таблицы заданных источников;
- общая панель видимости вторичных колонок;
- независимые Undo/Redo-истории вкладок;
- сохранение порядка записей и отключённая сортировка исходных таблиц;
- предварительная проверка безопасного контекста и File System Access API с
  пользовательской диагностикой вместо необработанной ошибки браузера.

Точные технические контракты и инварианты находятся только в
`Architecture.md`.

## 3. Владельцы основных контрактов

- `tabRegistry` — состав вкладок;
- schema — описание данных вкладки;
- `modelSerializer` — StorageModel ↔ BaseModel;
- `arrayShape` — форма и ориентация ARRAY;
- `modelCompute` — граф вычисляемых свойств;
- `rowLabel` — подписи строк;
- `COMMON_TABLE_OPTIONS` — общие опции Tabulator;
- `constraintValidator` и реестр ограничений — ограничения отдельных значений;
- `modelRegistry` и валидаторы вкладок — условные, межполевые и модельные
  ограничения;
- `diagnosticService` — объединённая диагностика;
- `selectionService` — выбранное задание и версия его данных;
- `selectionContextService` — активная таблица для операций панели;
- `fileSystemAccessSupport` — проверка клиентского окружения и классификация
  ошибок системных файловых диалогов.

## 4. Сборка, тесты и развёртывание

Для разработки используется Vite dev server. Production-сборка формируется в
`dist`. Команды и Windows-сценарии описаны в `../README.md`.

Реализованы сценарии:

- dev-запуска;
- локальной проверки `npm test` и production-сборки;
- LAN-развёртывания через Caddy с внутренним HTTPS;
- автоматической сборки и публикации GitHub Pages.

Официально поддерживаемые клиенты — актуальные Google Chrome Desktop и
Microsoft Edge Desktop через HTTPS или localhost. Chromium SDK отдельно на
клиентском компьютере не устанавливается. Firefox, Safari и другие браузеры
не входят в подтверждённую клиентскую конфигурацию.

Предыдущая подтверждённая release-проверка включала успешные `npm test` и
production-сборку. Текущая проверка клиентского окружения покрыта автономными
Node.js-тестами; системные диалоги Chrome и Edge дополнительно требуют ручной
проверки на Windows.

## 5. Постоянные требования сопровождения

- При переносе математики из Julia-решателя сохраняется таблица соответствия
  источника и JavaScript-реализации; изменение источника требует повторной
  сверки и тестов.
- Для каждого элемента исходных данных явно рассматриваются пользовательские
  ограничения и ограничения математической или физической модели.
- Предметные ограничения не считаются исчерпывающе выводимыми из формата файла
  или типа значения.

## 6. Открытые работы
- Реализовать модельную проверку `MC-ELEMENTS-001`, определённую в разделе
  «Предметные ограничения исходных данных» файла `Architecture.md`, и добавить
  автоматический тест.
- При последующих изменениях перенесённых алгоритмов поддерживать их
  прослеживаемость до актуальной версии Julia-источника.

Примеры заданий и legacy-данные принадлежат проекту `solver`; они не входят в
сборку, Caddy-развёртывание или GitHub Pages проекта `web-gui`.

Новый функциональный объём начинается после определения затрагиваемых
компонентов, файлов, рисков и проверок по `IMPLEMENTATION_PROTOCOL.md`.

## 7. Приоритетные изменения

- Портировать из решателя solver функцию validator для проверки корректности 
  задания вершин объемного шестигранного элемента: src\core\03_kv.jl
- Обеспечить доступность ее использования при валидации геометрии в elements.
  Использовать исходные комментарии для заполнения description и локальных комментариев.
- Изменить ответственность за использование базовых библиотек: передать от solver к gui
  т.к. задание для решателя должно быть самодостаточным и не обращаться к внешним данным.
  Базовые библиотеки используются в gui для выбора нужных характеристик (библиотека свойств)
  и изменения настроек рещателя (рабочие константы). При этом назначенные в gui характеристики
  всегда прямо копируются (в виде файла) в подкаталог xapLibFMM/xapLibHTC для локальных 
  характеристик и, при необходимости, редактируются в gui или непосредственно в текстовом 
  файле характеристики. Это отличается от прежней схемы, когда отсутствующие характеристики 
  брались в решателе из базовой. Теперь решатель ничего не должен знать о базовых характеристиках.
- Решить вопрос о рациональной точке расположения библиотек по умолчанию: С одной стороны, они
  должны использоваться только из gui. С другой стороны, gui исходно не требует установки, в 
  отличии от clark.app и логично располагать ее там. Но тогда будет ненужная привязка по рксурсам:
  решатель - gui, включая процелуру импорта из legacy формата. Видимо потребуется езависимое 
  установка - импорт - хранение. Хотя ярлык на запуск импорта по прежнему расположится в папке
  запуска на рабочем столе. Процедура коснется только точки размещения библиотек.
- Решить вопрос об эффективности одного из двух способов определения является ли используемая 
  характеристика измененной относительо базовой: (а) введение дополнительного поля (б) прямое
  сравненние с базовой.