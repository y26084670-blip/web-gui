{
  "name": "samp_app4",
  "private": true,
  "version": "4.18.0",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "assets:prepare": "node scripts/default-library-assets.mjs prepare && node scripts/prepare-examples-config.mjs",
    "assets:verify": "node scripts/default-library-assets.mjs verify",
    "dev": "npm run assets:prepare && vite",
    "build": "npm run assets:prepare && vite build && npm run assets:verify",
    "build:release": "npm run assets:prepare && vite build --mode production && npm run assets:verify",
    "build:pages": "npm run assets:prepare && vite build --mode production --base=/web-gui/ && npm run assets:verify",
    "preview": "vite preview"
  },
  "dependencies": {
    "chart.js": "^4.5.1",
    "solid-js": "^1.9.12",
    "tabulator-tables": "^6.5.0",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^8.0.12",
    "vite-plugin-solid": "^2.11.12"
  },
  "description": "--- SPA с вкладками в таб-элементе + solidJS + Tabulator-tables",
  "main": "vite.config.js",
  "directories": { "doc": "docs" },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
